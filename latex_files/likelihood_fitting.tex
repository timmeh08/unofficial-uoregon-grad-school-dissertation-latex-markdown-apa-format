%--------------------------------------------------------------------------------%
% start my deeper dive into fitting here, documenting the motivation for fitting and the details of the fit itself.
Most analyses with the ATLAS experiment are essentially counting experiments, where the number of events observed in data is compared to the number of events expected from the background-only hypothesis and the signal-plus-background hypothesis. 
The likelihood function is a powerful tool that quantifies the agreement between observed data and these hypotheses, taking into account both statistical and systematic uncertainties. 
By maximizing the likelihood function, we can estimate the parameters of interest, such as the signal strength, and determine how well our data supports the presence of a signal. 
In this section, the fitting techniques used in this analysis will be described, using the statistical framework from TRExFitter and data/MC events from the dark photon analysis as described in appendix~\ref{app:dark_photon_mc}.

The likelihood function is written as:

\begin{equation}\label{eq:likelihood}
\mathcal{L}(\mu,\boldsymbol{\theta}, \gamma, \tau)
=
\prod_{i=1}^{N_{\text{bins}}}
\mathrm{Pois}\!\left(N_i \mid \mu s_i(\boldsymbol{\theta}) +b_i(\boldsymbol{\theta},\gamma,\tau)\right)
\;
\prod_{\theta\in\boldsymbol{\theta}}
\frac{1}{\sqrt{2\pi}}e^{-\theta_k^2/2}
\;
\prod_{i=1}^{N_{\text{bins}}}
G\!\left(n^{MC}_i \mid \gamma_i\right).
\end{equation}

where $\mu$ is the signal strength, $\boldsymbol{\theta}$ are the nuisance parameters associated with systematic uncertainties, $\gamma$ are the parameters associated with the MC statistical uncertainties, and $\tau$ are the background normalization factors.
The first product runs over the terms per bin of the histograms used in the fit.
This includes the number of data events, number of expected signal events $s_i$, and number of expected background events $b_i$.
$\mu$ scales the signal yield, while the nuisance parameters $\boldsymbol{\theta}$ affect both signal and background yields, and the $\gamma$ parameters affect only the background yields.
The second product runs over the nuisance parameters, or systematic uncertainties, which are constrained by Gaussian distributions centered at zero with a width of one.
Examples of systematic uncertainties include those related to the reconstruction of physics objects or detector related effects, calibration, theoretical modeling, MC modeling, and data-driven background estimation.
This adds degrees of freedom to the fit, allowing the fit to adjust the expected yields within the uncertainties to better match the observed data.
Nuisance parameters can affect the shape of the distributions used in the fit, as well as the overall normalization.
Systematic uncertainties in the dark photon analysis are described in more detail in Section \ref{sec:systs}.
Ideally, MC simulations would have infinite statistics, but in practice they are limited by computational resources, leading to statistical uncertainties in the predicted yields.
The final product in the likelihood function accounts for the statistical uncertainties in the MC predictions, which can be modeled as Poisson or Gaussian distributions depending on the number of MC events in each bin.
$\gamma$ nuisance parameters are introduced in this final product, and scale the total signal plus background yields in each bin.
It is important to note that there is only one $\gamma$ parameter per bin, which means that the MC statistical uncertainty is treated as a single source of uncertainty that affects all processes in that bin, rather than having separate $\gamma$ parameters for each process.
It is common to use the negative log-likelihood (NLL) for numerical stability and convenience, as it transforms the product of probabilities into a sum, which is easier to handle computationally.
The NLL is defined as:

\begin{equation}\label{eq:nll}
    -\ln\mathcal{L}(\mu,\boldsymbol{\theta}, \gamma, \tau) = -\sum_{i=1}^{N_{\text{bins}}} \ln \mathrm{Pois}\!\left(N_i \mid \mu s_i(\boldsymbol{\theta}) +b_i(\boldsymbol{\theta},\gamma,\tau)\right) - \sum_{\theta\in\boldsymbol{\theta}} \ln \frac{1}{\sqrt{2\pi}}e^{-\theta_k^2/2} - \sum_{i=1} \ln G\!\left(n^{MC}_i \mid \gamma_i\right).
\end{equation}

Maximizing the likelihood function (or equivalently minimizing the NLL) allows us to find the best-fit values of the parameters, including the signal strength $\mu$ and the nuisance parameters $\boldsymbol{\theta}$ and $\gamma$.
The parameter of interest (POI) is the signal strength $\mu$, which quantifies the presence of a signal in the data, and in the case of the dark photon analysis, the branching ratio $BR(H\rightarrow \gamma \gamma_d)$ is considered a free parameter, as well as the k-factors used for the normalization of $Z\gamma$ and $W\gamma$ background.

\subsection{TRExFitter Framework}

The likelihood function is implemented and maximized in the dark photon analysis using the TRExFitter framework\footnote{\href{https://trexfitter-docs.web.cern.ch/trexfitter-docs/latest/}{TRExFitter documentation}}, which is a software tool developed by the ATLAS collaboration for performing statistical analysis in particle physics.
TRExFitter is run using a configuration file that specifies the details of the fit, including the input histograms, the parameters to be fitted, and the systematic uncertainties to be included.
A workspace is created for the fit, containing the the parameters of interest, the physics samples, and the systematic uncertainties.
The fit is performed on the workspace, either with real data or with Asimov samples.
Asimov samples are generated based on the expected yields from the MC templates, without any statistical fluctuations, and are used to evaluate the expected sensitivity of the analysis \cite{Cowan_2011}.
The fit results include the best-fit values of the parameters, a breakdown of their uncertainties, and the correlations between the parameters.
There are multiple regions defined and fed into the fit.
A signal region (SR) is optimized with selections that enhance the signal contribution, while control regions (CRs) are defined to constrain the background contributions.
Validation regions (VRs) can also be defined to check the modeling of the backgrounds.
They are constrained by the fit, but the fit parameters are used to check for closure in the region.
The various regions used in the dark photon analysis are described in more detail in section \ref{sec:selections}.
Prefit and postfit distributions can be produced to visualize the agreement between the data and the fitted model in all regions, as well as the impact of the systematic uncertainties on the fit results.
Diagnostic plots such as the ranking of the nuisance parameters and the correlation matrix can be produced to understand the impact of each systematic uncertainty on the fit results and the correlations between the parameters.
When two or more parameters are correlated, checking if the correlation is expected based on the analysis or the fit configuration is important, as unexpected correlations could indicate issues with the fit or the modeling of the systematic uncertainties.
In the ranking, the impact of each nuisance parameter on the signal strength $\mu$ is shown, which indicates how much the signal strength would change if that nuisance parameter were fixed to its prefit value.
Understanding which systematics have the largest impact on the POI or the fit results can help identify areas of improvement in the analysis, or allow for corrections to the fit configuration or the modeling of the systematic uncertainties.
Pull plots can also be produced to show the pulls of the fitted parameters, which indicate how much the fitted values deviate from their expected values based on the input MC templates and the data.
Ideally, the nuisance parameters should center around zero, and can be slightly constrained by the fit, but they should not be pulled to extreme values, which could indicate issues with the fit configuration or the modeling of the systematic uncertainties.
Expected or observed limits on the POI can be extracted from the fit, providing a quantitative measure of the sensitivity of the analysis on the Higgs to photon and dark photon decay.
Examples of the various diagnostic plots and fit results will be shown in the next section, where a walkthrough of the fitting procedure in the dark photon analysis will be provided.

\subsection{Walkthrough of the dark photon fit}

This section provides a walkthrough of the fitting procedure in the dark photon analysis, using the TRExFitter framework and the likelihood function described above.
Note that theoretical uncertainties are not included in the plots shown in this section, but they are included in section \ref{sec:simfit} for the final fit results of the analysis.
There are two fits conducted in the dark photon analysis: a signal-plus-background fit that is blinded, and a signal-plus-background fit that is unblinded.
Fitting is performed in bins of transverse mass $m_T$, calculated using the photon and the \ETmiss.
The blinded fit is performed on Asimov data in the signal region, with real data in the control regions.
The unblinded fit is performed on real data in both the signal and control regions, with the data summarized in section \ref{sec:samples}.

As seen in Fig.~\ref{fig:asimov_prefit}, asimov data matches exactly with the expected yields from the background MC templates, with no statistical fluctuations.
This allows for the evaluation of the expected sensitivity of the analysis without biasing the results by looking at the real data in the signal region, as well as acting as a check of the fit configuration and the implementation of the systematic uncertainties.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=.45\textwidth]{../images/dark_photon/images/tim_fit/SR_asimov_dec31.png}
    \caption{Prefit distribution of the transverse mass $m_T$ in the signal region, with Asimov sample.}
    \label{fig:asimov_prefit}
\end{figure}

\subsubsection{Control Regions}

The various backgrounds in an analysis can be very correlated, and it can be difficult to disentangle their contributions in the signal region.
One way to address this is to define control regions that are enriched in specific backgrounds, and use the data in these control regions to constrain the normalizations of the backgrounds in the fit.
These backgrounds can then be free to ``float" in the fit, and the fit will adjust their normalization factors to best match the data in the control regions, which in turn constrains their contributions in the signal region.
This is fitting is done simultaneously with the fit in the signal region for the dark photon analysis.
Ideally, the control regions should be designed to be as orthogonal as possible to the signal region to avoid signal contamination, while still being close enough in phase space to the signal region to ensure that the background compositions are similar.

There are three control regions defined in the dark photon analysis, each enriched with a specific background: The one and two muon control regions, which are enriched in $W\gamma$ and $Z\gamma$ backgrounds respectively, and the low \ETmiss significance control region, which is used to constrain the jets faking photons background.
These regions are defined in section \ref{sec:selections}.
An example of the prefit distributions in the CRs can be seen in Fig.~\ref{fig:cr_prefit}, where the data is compared to the yields from the background MC templates.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=.45\textwidth]{../images/dark_photon/images/tim_fit/clean_OneMuCR_dec31.pdf}
    \includegraphics[width=.45\textwidth]{../images/dark_photon/images/tim_fit/clean_TwoMuCR_1bin_dec31.pdf}
    \includegraphics[width=.45\textwidth]{../images/dark_photon/images/tim_fit/clean_LowMETSigCR_dec31.pdf}
    \caption{Prefit distributions of the transverse mass $m_T$ in the control regions.}
    \label{fig:cr_prefit}
\end{figure}


\subsubsection{Impact of the jets faking photons background}

The low \ETmiss significance control region was originally a validation region to check the modeling of the jets faking photons background.
During fitting, however, the jets faking photons background was found to have a large impact on the fit results.
The uncertainty on the fake factor was significant, surpassing the statistical uncertainties and all other systematic uncertainties.
Correlations between the jets faking photons background and the signal strength were observed, as well as with the normalization factors of the $W\gamma$ and $Z\gamma$ backgrounds.
The breakdown of the nuisance parameter ranking in the fit, as well as the correlation matrix are shown in Fig.~\ref{fig:ranking_dec31}.


\begin{figure}[htbp]
    \centering
    \includegraphics[width=.35\textwidth]{../images/dark_photon/images/tim_fit/clean_Ranking_BRHyyd_Breakdown_dec31.pdf}
    \includegraphics[width=.45\textwidth]{../images/dark_photon/images/tim_fit/clean_CorrMatrix_dec31.pdf}
    \caption{Ranking of the nuisance parameters in the fit, showing the impact of each nuisance parameter on the signal strength $\mu$ (left). Correlation matrix of the fit parameters (right). Both plots are with the low \ETmiss significance validation region included in the fit.}
    \label{fig:ranking_dec31}
\end{figure}

It was proposed to transform the low \ETmiss significance validation region into a control region to constrain the jets faking photons background, and this was implemented in the fit.
The argument for this was that if the low \ETmiss significance validation region is stable and well modeled, it is then safe to rerun the fit with this region included as a control region, which would help constrain the jets faking photons background and reduce its impact on the fit results.
As seen in Fig.~\ref{fig:pre_post_VR}, the fit confirms that the low \ETmiss significance validation region is stable, and can be safely used as a control region.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=.45\textwidth]{../images/dark_photon/images/tim_fit/clean_LowMETSigVR_dec31.pdf}
    \includegraphics[width=.45\textwidth]{../images/dark_photon/images/tim_fit/clean_LowMETSigVR_postFit_dec31.pdf}
    \caption{Prefit (left) and postfit (right) distributions of the transverse mass $m_T$ in the low \ETmiss significance validation region. The fit confirms that the low \ETmiss significance region is stable, and can be safely used to constrain the jets faking photons background.}
    \label{fig:pre_post_VR}
\end{figure}

The low \ETmiss significance control region was then included in the fit, and studied with data and Asimov samples.
Postfit distributions of the low \ETmiss significance control region can be seen in Fig.~\ref{fig:cr_postfit}.
The background expectations are stable with all configurations of the fit, and the data is well modeled.
It can be seen in Fig.~\ref{fig:ranking_jets} that the jets faking photons fake factor uncertainty is better constrained, and the correlations with the signal strength and the normalization factors are reduced.
Along with the low $\Delta\phi(E^{miss}_T, E^{\gamma}_T)$ VR as described in section~\ref{sec:selections}, the low \ETmiss significance control region also serves as a cross check of the modelling of the jets faking photons background and the \ETmiss significance variable.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=.45\textwidth]{../images/dark_photon/images/tim_fit/clean_LowMETSigVR_postFit.pdf}
    \includegraphics[width=.45\textwidth]{../images/dark_photon/images/tim_fit/clean_LowMETSigVR_postFit_asimov.pdf}
    \caption{Postfit distributions of the transverse mass $m_T$ in the low \ETmiss significance control region with data (left) and Asimov samples (right). The background expectations are stable with all configurations of the fit, and the data is well modeled.}
    \label{fig:cr_postfit}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=.35\textwidth]{../images/dark_photon/images/tim_fit/clean_Ranking_BRHyyd_Breakdown.pdf}
    \includegraphics[width=.45\textwidth]{../images/dark_photon/images/tim_fit/clean_CorrMatrix.pdf}
    \caption{Ranking of the nuisance parameters in the fit after including the low \ETmiss significance control region, showing the impact of each nuisance parameter on the signal strength $\mu$ (right). Correlation matrix of the fit parameters, showing the correlations between the signal strength $\mu$ and the normalization factors of the backgrounds (left). Both plots are with the low \ETmiss significance control region included in the fit.}
    \label{fig:ranking_jets}
\end{figure}

Another check of how the fit compares with and without the low \ETmiss significance control region is to look at the normalization factor pulls of the backgrounds.
Prefit values are always at 1, and are constrained by data in the fit. 
Even though the two normalization factors should mostly be constrained by the one and two muon control regions, as said above, there were correlations between the backgrounds, and the jets faking photons background had a large impact on the fit results.
Now that the jets faking photons background is better constrained, the normalization factors should still be consistent with the values obtained without the low \ETmiss significance control region.
This is indeed the case, as seen in Fig.~\ref{fig:pulls}, where the normalization factor pulls of the $W\gamma$ and $Z\gamma$ backgrounds are consistent between the fit with and without the low \ETmiss significance control region.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=.45\textwidth]{../images/dark_photon/images/tim_fit/clean_NormFactors_CR.pdf}
    \includegraphics[width=.45\textwidth]{../images/dark_photon/images/tim_fit/clean_NormFactors_dec31.pdf}
    \caption{Normalization factor pulls of the $W\gamma$ and $Z\gamma$ backgrounds with the low \ETmiss significance control region included in the fit (left) and without it (right). The normalization factors are consistent between the two fits, showing that the inclusion of the low \ETmiss significance control region does not bias the fit results.}
    \label{fig:pulls}
\end{figure}

The final metric to compare the fit results with and without the low \ETmiss significance control region is the expected limits on the branching ratio $\mathcal{B}(H\to\gamma\gamma_d)$.
Expected limits were extracted from the blinded fit with and without the low \ETmiss significance control region, and are shown in Table~\ref{tab:limits}.


\begin{table}[htbp]
    \centering
    \begin{tabular}{lccccc}
        \hline
        Fit Configuration & $-2\sigma$ & $-1\sigma$ & Median & $+1\sigma$ & $+2\sigma$ \\
        \hline
        With low \ETmiss significance CR & .0049 & .0066 & .0095 & .014 & .020 \\
        Without low \ETmiss significance CR & .0059 & .008 & .011 & .016 & .023 \\
        \hline
    \end{tabular}
    \caption{Expected 95\% CL limits on $\mathcal{B}(H\to\gamma\gamma_d)$ at $-2\sigma$, $-1\sigma$, median, $+1\sigma$, and $+2\sigma$, with and without the low \ETmiss significance control region included in the fit. There is a roughly 10\% improvement in the expected limits when the low \ETmiss significance control region is included.}
    \label{tab:limits}
\end{table}

In summary, the inclusion of the low \ETmiss significance control region in the fit has a significant impact on the fit results, as it helps constrain the jets faking photons background, decouples it from the signal strength and the normalization factors of the other backgrounds, and improves the expected limits on the branching ratio $\mathcal{B}(H\to\gamma\gamma_d)$ by roughly 10\%.
The full fitting procedure and results for the blinded and unblinded fits are described in the next sections.

